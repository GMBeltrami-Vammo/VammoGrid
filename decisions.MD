# VammoGrid — Decision Log

Traceability record of architectural and product decisions made during the build.
Each entry records what was decided, why, and when.

---

## Architecture Decisions

| # | Decision | Chosen | Alternatives Considered | Rationale |
|---|---|---|---|---|
| 1 | Framework | Next.js 15 (App Router) | Vite + React SPA, Remix | Vercel-native zero-config deploy; API routes provide server-side Metabase proxy so credentials never reach the browser; TypeScript first |
| 2 | UI library | shadcn/ui + Tailwind CSS v4 | MUI, Ant Design, Chakra | Unstyled headless primitives owned by the codebase (no vendor lock); Tailwind v4 already bundled with create-next-app; fast to compose for internal tools |
| 3 | Charts | Recharts | Chart.js, Nivo, Victory | React-native, TypeScript-typed, excellent line/area/bar support for DOH and consumption charts; lighter than Nivo |
| 4 | Client data fetching | TanStack Query v5 | SWR, Redux Toolkit Query, fetch | Declarative server-state caching; staleTime can be aligned with the API cache TTL; built-in loading/error/refetch states |
| 5 | Tables | TanStack Table v8 | AG Grid (overkill), react-table v7 | Sortable + filterable with typed column definitions; headless — uses shadcn/ui Table for rendering; same ecosystem as TanStack Query |
| 6 | Authentication | None (public, unlisted URL) | NextAuth.js + credentials, Supabase Auth | Internal MVP; keeping the URL unlisted is sufficient for now; NextAuth can be added later without refactor (session provider already available in Next.js) |
| 7 | Primary data source (V1) | Metabase API proxy | ClickHouse direct, CSV export | Reuses existing Metabase question #27759; API key auth (`X-Api-Key` header) — simpler and more stable than session tokens (no expiry, no re-auth logic needed) |
| 8 | Primary data source (V2) | ClickHouse direct | Metabase only | ClickHouse is the actual warehouse behind all Metabase numbers; direct queries unlock SO history for forecasting and are more performant |
| 9 | Deployment region | Vercel GRU (São Paulo) | US-East, Auto | Low latency to metabase.vammo.com which is Brazil-hosted; reduces p99 on every data fetch |
| 10 | Primary data source (V3) | ClickHouse direct ONLY — Metabase fully removed | Keep both (fallback), Metabase only | Metabase's REST fallback cost ~2-3x the round-trips per page load (a 2000-row native-query cap forced the forecast alone into a DISTINCT + ~15 batched queries); confirmed CLICKHOUSE_* reachable from Vercel gru1, so the fallback had no remaining purpose. Deleted `lib/metabase/*`, the two `/api/metabase/*` routes, the dormant V1 client-architecture (`useInventory`/`useConsumption`/`useAlerts`/`useProjections`/`FilterContext`/`lib/projection.ts`/`components/inventory/*` island — zero live importers), and the admin "Recuperação" tab (wrote to `fleet.sku_params`, which the engine never read — superseded by `fleet.sku_policy`). Dropped `fleet.piece_stock_hub[_monthly]` (Metabase-sourced snapshot cron, unread by the app) and `fleet.sku_params`. |
| 11 | Config/state storage | ClickHouse ONLY — Supabase fully removed | Keep Supabase for editable config, split ClickHouse (facts) / Supabase (config) | Discovered the Supabase project was never VammoGrid-specific — a shared multi-tenant Postgres instance also running goBuy/Lumen (`finance`), a CX/Horus RAG assistant (`cx`/`public`), Kustomer sync, LLM-cost governance, and IoT alerting; VammoGrid's actual footprint (`fleet` schema) was 5 live tables / ~843 rows / 0 functions / 0 FKs — tiny and structurally flat (already a pure ETL-landing shape, not relational). User will manage the Supabase side directly; this decision covers removing the app's *dependency* only. Migrated `sku_policy`, `purchase_order`, `part_compat`, `fleet_info`, `job_run` → `dev.fleet_*` in ClickHouse (verified: exact row-count match post-copy). Two tables (`fleet.test_part_bike`, `fleet.test_part_service_order`, 1,287 rows) were found to have **zero references anywhere in VammoGrid's code** — not migrated, flagged to the user as possibly belonging to a different project against the same schema. `fleet.piece_stock_hub`/`_monthly`, `fleet.sku_params`, and the migration-file-only tables (`hub`, `transfer_constraint`, `forecast_override`, `plan_run`, `plan_line`) were confirmed already gone from the live schema (consistent with decision #10) — nothing to migrate. Mutation model: ClickHouse has no OLTP-speed row UPDATE/DELETE, so `dev.fleet_*` tables are `ReplacingMergeTree(updated_at)` + a soft-delete flag (read via `FINAL`; a write = a fresh full-row insert) — table sizes are tiny (≤ 1,000 rows) so `FINAL`'s cost is a non-issue. Every write also appends a per-field diff to a new shared `dev.fleet_audit_log` (append-only `MergeTree`) — "editable and logged" as one reusable mechanism rather than one per table. `purchase_order.id` moved from a Postgres bigint sequence to an app-generated UUID string (ClickHouse has no sequences). Along the way, found and fixed a real pre-existing bug: `middleware.ts`'s route-exclusion matcher was missing `/api/orders/sync`, so the daily cron's own `CRON_SECRET` check was unreachable — an unauthenticated request (exactly what a cron sends) was 401'd by middleware before the route ever ran. |
| 12 | Order-orchestration platform | Built per `docs/superpowers/specs/2026-07-01-order-orchestration-platform-design.md` (7 sub-projects, A→B→D→C→E→F→G) | Bigger-bang rewrite; deferring pieces | Spec approved after two review rounds. Implemented all 7: **(A)** default SKU universe = 139 reference SKUs in `dev.fleet_sku_scope`, narrowed app-wide in `loadPlanningInputs`/`loadSkuView` (fail-open when empty; `ignoreSkuSelection` bypasses for the "Lista completa" tab + deep links); editable on the SKUs page. **(B)** global service-level tier (Base/Padrão/Conservador = 95/97/99% → z, in `dev.fleet_global_settings`) overriding per-ABC z; lead-time σ → combined-variance safety `√(σ_d²·L + demand̄²·σ_LT²)`; per-hub max-stock caps (alert-only, no engine clamp); ROP-in-DOH on SKU detail; **the elaboration rule** (`findElaborationTrigger`: first day DOH<75 → maritime if the monthly-batch sea order arrives in time, else air, else air-flagged-late) — now Compras' entire reorder logic, computed on load, **written only on explicit human confirm** (`createElaboratedOrder` → a `prep_status='elaborado'` draft; no cron writes); national/international toggle. The old statistical ROP stays on the SKU detail only. **(D)** `prep_status` lifecycle (elaborado→enviado→feito; drafts excluded from projected inbound via `countsAsInbound`); pedido detail page `/pedidos/[vo]` (grouped lines + summary + audit-log history + prep transitions); SKU↔pedido cross-links. **(C)** Semanas heatmap: coverage scenario (baseline/air/sea/complete — synthetic order injection), service-level-tier DOH coloring floor, heat-filter + horizon (8/12/16/20) + DOH↔units toggle, extrapolated-week greying. **(E)** steady fleet-growth projection + Visão Geral panel (reads `dev.fleet_info`; NOT wired into the ML forecast). **(F)** frota manual ledgers (`dev.fleet_bike_{sales,order}_log`). **(G)** motos-paradas registry (`dev.fleet_backlog_bike_log`) + a heatmap "Backlog" demand-uplift toggle (extra% ≈ parado/fleet, reusing the forecast scaler — no BOM). All new state is ClickHouse `dev.fleet_*` (no Supabase, per #11), every edit audit-logged via `upsertFleetRow`/`softDeleteFleetRow`; new columns added via idempotent `ALTER TABLE ADD COLUMN IF NOT EXISTS` (run by `provisionFleetTables`, invoked by the seed route). `dev.fleet_sku_scope` seeded via a one-off GET/POST route (isHead-gated) to be deleted after running. |
| 13 | Post-launch UX + schema refinements | Frota = fleet-size/growth chart only; Compras = "Novo Pedido" builder; compat models collapsed to CPX/COMFORT; manual SKU creation; dd-mm-YYYY date inputs; delete-pedido; permanent provisioning route | Per-variant compat flags / bike-sales+order ledgers / per-row elaboration | Iterating on the platform from real use. **Frota**: dropped the vendas/pedidos-de-motos manual ledgers from the tab (deleted `FrotaPanel` + its actions + the `dev.fleet_bike_{sales,order}_log` tables) — the tab now shows only the per-model fleet-size/growth chart. **Compras → Novo Pedido**: replaced the per-row "Elaborar" flow (`createElaboratedOrder` removed) with a builder — per-SKU include checkbox + editable qty, ONE global modal for the whole order, and "Criar pedido" (`createPedido`) writing a single pedido (a generated shared `NP-YYYYMMDD-XXXX` VO, N lines, `prep_status='elaborado'`). **Compat consolidation**: `BIKE_MODELS` collapsed from 9 per-variant flags to just `cpx`/`comfort` (VS line dropped); added `cpx`/`comfort` Bool columns to `dev.fleet_part_compat`; a shared `deriveModels()` folds legacy per-variant columns in on read (fallback) while new writes set the two flags directly. **Manual SKU**: "Adicionar SKU" on the SKUs page (`createSku`) writes a `dev.fleet_sku_policy` row (lead sea/air, national/international, default modal, ABC, name) + an active `dev.fleet_sku_scope` row; added `sku_name` to `sku_policy` (the snapshot can't name a SKU not yet in inventory); the SKUs page unions policy-only SKUs so they show immediately (0 stock until inventory lands). **Dates**: a `DateField` component (text, DD-MM-YYYY ↔ ISO, pure helpers `parseDisplayDate`/`isoToDisplayDate` in `format.ts`, unit-tested) replaces every editable `<input type=date>` (native ones rendered MM/DD/YYYY in en-US browsers). **Delete pedido**: `deletePedido(ids)` soft-deletes all lines of a VO, wired to the pedido list card + a `DeletePedidoButton` on the detail page. **Provisioning**: since the one-off seed route was deleted (leaving `provisionFleetTables` with no caller), added a permanent idempotent isHead-gated `GET /api/admin/provision-fleet` to apply `CREATE`/`ADD COLUMN IF NOT EXISTS` after any schema change. |
| 14 | Canonical DOH = stock ÷ next-7-day avg demand | `forwardAvgDemand(timeline, day, 7)` as the single days-of-cover rate everywhere | Per-day single-demand divisor (old); 30-day trailing avg (old `dohNow`) | DOH was computed three different ways (projection chart & heatmap ÷ that single day's demand; `dohNow` ÷ a 30-day mean; elaboration ÷ single day) — a single spiky/zero forecast day made a cell read e.g. "59 DOH" spuriously (the reported VM-01-CAR0-3501 case) and made the scenario breach-detection erratic. Unified on ONE definition (workspace "one canonical definition per concept" rule): **coverage = stock ÷ the average daily demand of the NEXT 7 days** (`forwardAvgDemand`, exported from `projection.ts`). Applied to the Estoque `ProjectionChart` per-point DOH + band, the Semanas heatmap cells, the heatmap's `firstBreachDay` (so "buy-when-needed" injections target the displayed number), `projectStream.dohNow` (the "Cobertura atual" KPI now equals the chart's start), and the Compras `findElaborationTrigger` <75 scan. `dailyDemand` (the "Consumo/dia" figure) stays a 30-day mean — a separate "typical consumption" number, not a coverage divisor. **Semanas UX** (same batch): each cell has a cursor-following hover tooltip (stock, coverage vs floor, registered "pedido em trânsito" vs suggested "pedido sugerido" arrivals split by modal, recovery income, comprar-até/ruptura flags) — `WeekCell` gained `arrReg`/`arrSug`; recovery no longer recolors the whole cell (only a Recycle-icon detail, `weekCellClass` dropped its recovery branch); Frota chart got a distinct categorical palette (the `--chart-*` ramp is all one brand-cyan hue → models were indistinguishable blues). Could not reproduce the exact SKU end-to-end (no ClickHouse creds in-session); reproduced the metric defect in a unit test (`doh.test.ts`) instead. |
| 15 | Admin-configurable purchase criteria (DOH 75 / ROP) + Visão Geral load fix | `dev.fleet_global_settings.purchase_criteria = {mode:'doh'|'rop', dohThreshold}` (default DOH 75); tier keeps only the z-score role | Fixed DOH floor 60 per tier; per-page thresholds | **Load bug**: after the compat consolidation, stale model keys (`cpx_preta`) lingered in the `vg:filter` cookie; `skuPasses` matched them against the new cpx/comfort sets → zero matches → every scope-narrowed page (dashboard "Visão Geral", Semanas, Compras, transfers, alertas, hub) silently emptied while the ignore-selection pages (Estoque, SKUs) still showed data. `parseFilterCookie` now drops unknown model keys; scope narrowing is fail-open (a scope that matches nothing in a non-empty catalog falls back to all stocks); dashboard empty-hint points at filters/scope when CH is connected. **Criteria**: replaced the tier-derived Semanas DOH floor (base 60/padrão 75/conservador 90) with ONE admin-configurable purchase criteria in `dev.fleet_global_settings` — `{mode, dohThreshold}`, default `{doh, 75}`. Two modes: **DOH** (request/color-low when projected coverage < N days, N editable) and **ROP** (when projected stock < estoque mínimo + estoque de segurança). Drives BOTH the Compras `findElaborationTrigger` AND the Semanas heatmap "low"/breach coloring + the scenario when-needed injection (per-hub ROP pro-rated by demand share). The service-level **tier** (Base/Padrão/Conservador) now governs ONLY the safety-stock z — its DOH-floor role is gone (`SERVICE_LEVEL_DOH_FLOOR` deleted). New `PurchaseCriteriaPanel` in Admin; `setPurchaseCriteria` action; `fetchPurchaseCriteria` source; exposed via `/api/fleet/global-settings` + `useServiceLevel`. Bumps the visible "request criteria" from 60 → 75 by default. |
| 16 | Novo Pedido builder upgrades + combined air/sea plan + windowed-chart DOH fix + clickable arrivals | Modal-aware suggested qty (air bridges until the monthly sea order; sea sustains ≥ threshold); rich Novo Pedido filters/selection; yellow "with suggestion" chart overlay | Single modal-agnostic order-up-to qty; whole-horizon air qty | **Chart DOH bug**: the D-30→D+30 window sliced the projection to 31 points, truncating the next-7-day DOH denominator near D+30 → a different coverage number than the full D0→D+150 chart. `ProjectionChart` now takes a full-horizon `rateSource` (indexed by absolute day) for the DOH denominator while still displaying the window; `StockWindowChart` passes `proj.timeline`. **Combined suggestion** (user's chosen model): `suggestModalQuantities` returns `{airQty, seaQty, airArrival, seaArrival}` — AIR = minimal units to hold DOH ≥ threshold ONLY from the air arrival until the next monthly (1st-of-month) SEA order lands (0 when sea arrives before any breach); SEA = the bulk topping up to (threshold + 30-day cadence) of cover. `ElaborationRow` carries both; the builder's suggested qty follows the chosen modal (flip resets), with a ✈/🚢 mini-line showing both. Unit-tested. **Novo Pedido UX**: sidebar renamed Compras→"Novo Pedido"; filters (Aéreo necessário / Marítimo by `suggestedModal`, DOH<N); explicit Selecionar-visíveis / Limpar-seleção; "Qtd sugerida" + InfoHint + per-row "sug. N · redefinir"; live order cost total; "Comprar até" (buy-by) column. **Estoque yellow line**: both SKU charts overlay a yellow "com pedido sugerido" projection (air+sea injected, re-projected via `projectSku`) — `ProjectionChart.suggestionTimeline`, threaded EstoqueView→StockWindowChart/ProjectionView. **Chart PO-arrival labels are clickable** → the pedido page; **Semanas cells with a real arriving order are clickable** → pedido, and the tooltip distinguishes "pedido já colocado" (VO, clickable) from "sugestão de compra (cenário)". |
| 17 | Hand-picked selection: chunked compact cookies (cap 100 → 1000) | Selection stored OUTSIDE `vg:filter`, in `vg:skus0..N` (base codes joined by `~`, no JSON/encoding), read+concatenated server-side | Bigger single cookie (≤ ~200, hits 4KB); per-user ClickHouse store (unbounded but async → navigation races + new infra) | The selection lived inside the JSON `vg:filter` cookie, capped at `MAX_SELECTED_SKUS=100` to stay under the ~4KB single-cookie limit — the user needs 200+. Moved `skus` out of `vg:filter` into a dedicated CHUNKED compact cookie set: `vg:skus0..vg:skus7` (`MAX_SKU_CHUNKS=8`), each ≤ 3500 bytes, base codes joined by `~` (URL-safe `[A-Z0-9-]` → no JSON quotes/percent-encoding → ~140 codes/chunk). `encodeSkuChunks`/`decodeSkuChunks` (pure, unit-tested round-trip to 500) in `filter.ts`; `writeSkusCookies` (client, clears stale chunks) in `applyFilter.ts`; the loader reads the chunks via `readSkuChunkCookies` and merges into `filter.skus`. `parseFilterCookie` now ignores any legacy `skus` in `vg:filter`. Kept it COOKIE-based (not a server store) so it stays synchronous — no navigation race between selecting and the server-rendered analyses reading it — and per-browser (unchanged semantics). Cap raised to 1000. All writers updated (SkuTable, FilterBar, SkuFilterToggle). Existing in-cookie selections reset on deploy (acceptable per "even if that means losing the cookie"). |
| 18 | SKUs page = "visible to analyses" manager; selection dominates; top filters drive the checkboxes | Selection (when present) OVERRIDES scope + top filter for what analyses show; SKUs page always lists every SKU; top filters check/uncheck | Selection additive (∩ scope ∩ filter) — left "checked but not visible" edge cases | User's model: on the SKUs page every SKU is always listed, and a checkbox means "this SKU is visible to the other pages," with the top filters (Com previsão / Modelos / categoria) checking/unchecking in bulk. Implemented: **(1)** `loadPlanningInputs`/`computeSnapshot`/`safeComputeSnapshot` gained an `ignoreFilter` param; the SKUs page calls `safeComputeSnapshot(true, true)` → lists the FULL catalog (neither scope nor top filter hides rows). **(2)** Narrowing is now: `ignoreFilter` → full catalog; else a non-empty **selection dominates** → analyses show EXACTLY the selected SKUs (from the full catalog, overriding the default scope + top filter) so checked ⟺ visible; else → scope ∩ top-filter (unchanged default). **(3)** The SKUs page computes `matchingSkus` (a 2nd `safeComputeSnapshot(true,false)` when a top filter is active) + a `filterSignature`; `SkuTable` syncs the selection to `matchingSkus` via a `useEffect([filterSignature])` — applying/clearing a top filter checks/unchecks the matching SKUs; manual checkbox edits persist until the next filter change. The per-row **Escopo** toggle (default universe) stays as the fallback when there's no selection. |
| 19 | Performance pass — identical results, snapshot-gated | Pure reuse/precompute/skip refactors, frozen by a pre-refactor characterization test | Approximations (lower injection caps, coarser bands) — rejected by user; per-user cross-request engine caching — deferred (cookie-dependent keys) | Full app review found the engine doing ~96·N 150-day projections per Semanas load (4 scenarios × ≤6 injection iterations × 4 scopes + finals), `forwardAvgDemand` called ~900·N× with an O(7) loop, the SKUs page running the whole engine twice, and `transferForAll` running on 4 pages that never read transfers. Fixes, each its own commit, ALL gated on a new `weekgrid.test.ts` characterization snapshot (full line-serialization of `buildAllScenarioGrids` over fixtures covering zero-demand/breacher-with-boundary-POs/repairable/long-sea-lead × {doh,rop} × {8,20} weeks — byte-identical throughout) + a seeded property test pinning `forwardAvgDemand` to the naive loop: **(1)** `forwardAvgDemand` O(1) via a WeakMap per-timeline table (entries computed with the verbatim naive summation — prefix sums could differ 1 ULP). **(2)** `projectGlobal` (shared `globalStream` path) — `computeElaborations` and the injection loop project 1 scope instead of 4. **(3)** weekgrid: `weekIndexFor` index math; projection horizon = `min(150, max(weeks*7+7, 30))`; `buildSharedContext`+`buildGridForScenario` — ONE baseline projection per SKU shared across scenarios (iteration 0 of the injection loop IS it), no-injection SKUs reuse baseline row objects → ~94% fewer day-ops for the no-breach majority. **(4)** transfers split out of `computeSnapshot` into `computeTransfers` (only dashboard + Transferências read them; React cache() keys on the FULL argument list — all internal callers pass both args explicitly). **(5)** SKUs page computes `matchingSkus` in-memory (`compatModels` exposed on `PlanningInputs`, same `skuPasses` predicate = full catalog ∩ top-filter) — second engine run deleted. **(6)** ops: 15s `AbortSignal.timeout` on ClickHouse fetches; tier change no longer busts the unrelated `policies` cache; Head-gated `GET /api/admin/refresh-cache?tag=forecast\|stock\|alerts\|shares\|recovery-rates\|all` for the warehouse caches that have no write-path bust. **(7)** client: Frota chart Recharts split to a dynamic-imported Inner (~120KB off the route); SkuTable + Semanas grid render 300 rows + "Mostrar mais" (selection/summary math stays on the full filtered set; `content-visibility` is a no-op on table rows); `useCompat`/`useFleetInfo` staleTime 60s→10min. **(8)** `readFleetRow(table, where)` (escaped WHERE + LIMIT 1, unit-tested builder; vitest aliases `server-only` to a stub) replaces read-whole-table-to-find-one-row in 9 actions. Explicitly assessed and SKIPPED: `projectStream`'s per-day sqrts (load-bearing — every point carries the band). |
| 20 | Rodada do review de stakeholders (itens 2, 3b, 5a, 6, 7a, 7b, 8) | Vitórias rápidas + Frota semanal ("Ambas") + regras por pedido via URL + snapshot congelado | Import xlsx/Drive (3a — recusado, mantida a regra ingestão-só-ClickHouse); Fornecedores (4b) e consolidação Lead Times→cadastro (4a) adiados | Triagem feita com o usuário sobre o review de validação. **(5a)** Heatmap ganhou a coluna "Hoje" (idx 0, dayOffset 0): posição atual, POs atrasadas (crédito dia-0, igual à projeção) e o flag pedir quando comprar-até ≤ hoje; `WeekMeta.idx` == índice do array; snapshot regenerado deliberadamente (diff = só a coluna nova). **(6)** Tabela SKUs: colunas consumo/dia + OSA/MOO/SBC, ordenação clicável (asc→desc→padrão, sobre o conjunto filtrado antes do corte de 300) e Exportar CSV do conjunto completo — o "relatório semanal de estoque" do item 1. **(7a/3b)** `dev.fleet_purchase_order` += `pedido_name`/`order_type`; nome + tipo Nacional/Internacional no builder e no form manual, editáveis no cabeçalho; lista com chips de tipo + busca por SKU/VO/nome; detail com título=nome e Tipo no summary. **(2, formato "Ambas")** Nova `dev.fleet_size_weekly` (segment, week_start, size): registros semanais REAIS por modelo + atalho "informar fim de mês" que interpola homogeneamente desde o último registro; o gráfico usa o passado real (fallback: retroprojeção) e ancora a projeção no último registro. Compatível com a decisão #13 (o que foi removido eram ledgers de vendas/pedidos de motos). NOTA mantida: frota NÃO alimenta a previsão de demanda (decisão #12) — meta comercial/churn (fase 2) melhorariam só a projeção da frota. **(7b)** `OrderRules {seaFloorDoh, seaCadenceDays, airPeriods[{fromOffset,minDoh}]}` vivendo na URL do Novo Pedido (`?rules=`, parseado+clampado): `findElaborationTrigger` ganhou `floorAt(day)` (piso variável no tempo), `suggestModalQuantities` ganhou `seaCadenceDays` (a cadência 30 hardcoded virou parâmetro) e `airFloorAt`; painel colapsável "Regras deste pedido" com badge. SEM conflito com a decisão #15: o Admin segue o default e Semanas segue no critério global. **(8)** `elaboration_snapshot` por linha, congelado no Criar pedido: {forecastAsOf, criteria mesclado, rules, suggestedQty/Modal, chosenQty}; bloco "Base da elaboração" no detail — habilita previsão×realizado (fase 2). **Backlog registrado**: Fornecedores (4b) com cadastro + vínculo SKU↔fornecedor (nac/int, prioritário) e compra nacional emergencial em DOH crítico; import de pedido via xlsx/Drive (3a — usuário optou por manter sync ClickHouse + formulário); consolidar a tela Lead Times no cadastro do SKU (4a); meta comercial + churn na projeção de frota; dashboards previsão×realizado sobre os snapshots. |
| 21 | Backlog do review: Fornecedores, Prev×Real, Import, Lead-times, Meta/Churn | Fazer TODOS os 5 itens restantes; Fornecedores como cadastro/vínculo (SEM 3º modal); import só xlsx; meta/churn em %; prev×real com 2 overlays | 3º modal no motor (recusado — "aéreo é o emergencial"); import via Google Drive (recusado — mais infra); fornecedor como fonte do lead time do SKU (recusado — mantidos os campos do SKU); prev×real também sugerido×pedido/custo (recusado — já no detail) | Rodada de backlog após o review. Decisões travadas com o usuário. **(4a)** Lead time editável por SKU no cadastro (Estoque), reusando `updateLeadTimePolicy` (`LeadTimePanel`); NavLink "Lead times" saiu do menu (tela em massa segue por URL). **(2 fase 2)** `dev.fleet_info` += `commercial_target_pct`/`churn_pct` (%/mês); helper puro `netMonthlyGrowthRate` = quando meta e/ou churn presentes, net = meta − churn substitui a taxa manual (lado ausente = 0 só quando o outro existe); ligado no `useMemo` do `FleetGrowthChart`; `upsertFleetInfo` passou a MESCLAR sobre o current (só sobrescreve meta/churn quando fornecidos, senão o editor inline de taxa os zeraria). Frota segue SEM alimentar a demanda (decisão #12). **(3a)** Import de pedido via `.xlsx`: dep `xlsx` (SheetJS 0.20.3 patched, dynamic-import), parse client-side, dialog com preview; parser puro `parseImportRows` (headers com aliases/acentos, SKU+Quantidade obrigatórias, lead por linha ou ETA−data ou default do modal); `createPedido` ganhou `source?` (default 'elaboracao'), o import grava `source='import'` — fonte distinta que o cron (replace-by-`source='clickhouse'`) nunca sobrescreve. Google Drive recusado. **(4b) Fornecedores SEM motor**: "aéreo é o emergencial" → NÃO há 3º modal. Novas `dev.fleet_supplier` + `dev.fleet_sku_supplier`; `purchase_order` += `supplier_id`/`supplier_name`. Página `/dashboard/fornecedores` (CRUD), painel de vínculos no cadastro do SKU (preferido/prioridade), seletor de fornecedor + botão "Por fornecedor" no Novo Pedido (`groupBySupplier` → um pedido por fornecedor preferencial), fornecedor exibido/buscável na lista e no detail. Helpers puros `preferredSupplierBySku`/`groupBySupplier` testados. Lead times do SKU permanecem no SKU (fornecedor não vira a fonte). **(8 fase 2)** Previsão × Realizado por pedido (`/pedidos/[vo]/previsao-realizado`, linkado do bloco "Base da elaboração"): usa o `forecastAsOf` congelado. Fontes novas `fetchForecastAsOf` (run histórico por `as_of_date`) e `consumption.ts` (consumo diário do ledger `stg_ims_r__ledger` USAGE%, agregado por sku_base); estoque real reusa `fetchStockHistory`. Montagem pura `buildPrevReal` (demanda previsto×real; estoque projetado [on-hand@as_of − Σ previsto, piso 0] × real; KPI realizado÷previsto). Gráficos Recharts lazy-loaded. **Ainda em aberto**: compra nacional emergencial automática (não construída — aéreo cobre); consolidar σ/ABC no cadastro; prev×real agregado multi-pedido. |
| 22 | Mega-rodada: N modais por fornecedor + filtros/seleção + Projeção Global + part number + Prev×Real por janela | N modais generalizados no motor MAS confinados ao builder/linhas/mini-heatmap (heatmap grande segue binário); seleção como única fonte + presets; simulação da Projeção Global efêmera (não afeta pedidos); part number ponta-a-ponta; janela do Prev×Real = [emissão→ETA] | Colapsar os 4 cenários do heatmap em 2 + células N-modais (recusado — estilhaçaria o snapshot de caracterização sem ganho: o motor é modal-agnóstico); DOH mínimo por modal PERSISTIDO (recusado — é só simulação na Projeção Global); 3º modal no motor do Novo Pedido como efetivo desde já (feito via modais do fornecedor, não hardcode) | Rodada grande a partir de uso real + um artefato paralelo ("Stockout Forecast"). Decisões travadas via AskUserQuestion. **N modais (F3/F4/F5)**: fornecedor expõe N modais (`dev.fleet_supplier_modal`, ex. VMoto Courier 15 / Aéreo 45 / Marítimo 105); `modalsForSupplier` (fallback aos leads legados sea/air), `suggestQuantities({projection, plans:ModalPlan[]})` generaliza o air-bridge/sea-bulk (lanes rápidas fazem ponte até a próxima chegada, a mais lenta faz order-up-to piso+cadência). **Insight-chave**: `projection.ts` nunca lê `modal` (timing = eta/lead), então um pedido N-modais = N recibos sintéticos → o N-modal fica no **builder + linhas (modal string livre) + mini-heatmap client-side** (`miniStrip.ts`: `projectFromSeed` reusa `projectStream`; `sampleMiniStrip`; `minDohWithin`), SEM tocar `weekgrid.ts`/`WeekCell {sea,air}`/`TransportModal`/o snapshot. Limitação aceita: um pedido 'courier' lido pelo heatmap grande é rotulado marítimo (timing certo). **Filtros (F1/F2)**: removidos os filtros do topo; a **seleção é a única fonte** das análises (chunk cookies `vg:skus0..7`), a página SKUs é o centro de controle com dimensões locais (categoria/modelos/previsão/nac-int/fornecedor/recuperável); **presets nomeados** em `dev.fleet_filter_preset`. **Cenários de demanda removidos** (ScenarioBar + ±% consumo + delay de PO) — o atraso agora é a ETA de cada pedido. **Semanas → "Projeção Global"** (rota mantida). **Novo Pedido (F5)**: só SKUs do fornecedor escolhido; filtro DOH reorganizado em **Cobertura** (horizonte) + **Estoque mínimo (DOH)** (aparece se algum dia na janela furar, via `minDohWithin`); painel "Modais deste pedido" (multi-select + frequência), qty por (SKU×modal) via `suggestQuantities`, mini-heatmap por linha; `createPedido` grava modal por linha; snapshot congela o plano. **Projeção Global (F6, tudo aditivo — snapshot intacto, o serializer só lê campos nomeados)**: `WeekGridRow` += recovery/turnaround, category, abcClass, openPos → coluna Consumo/dia com recuperação (%·d), badge "N pedidos" na coluna do SKU, filtros locais Categoria/Classe ABC, resumo stockout/semana clicável; botão **Exportar → Novo Pedido** (agrupa SKUs em risco por fornecedor preferido → deep link `?supplier=&skus=`); **painel de simulação EFÊMERO** (`simulateWeekGrids` server action: leads por fornecedor + pisos por modal, recomputa só a página, NÃO persiste, NÃO afeta Novo Pedido/Pedidos); `WeekCell` += `arrNat` → marcador âmbar de chegada NACIONAL (`OpenPurchaseOrder.orderType`). **Fornecedores**: editor de modais movido para dentro do form de edição (descoberta) + badge "N modais". **Notas P3**: `supplier_part_number` no vínculo SKU↔fornecedor (edição inline no cadastro) + `purchase_order.part_number` gravado por linha, lido no import (parser + preview) e mostrado no detalhe. **Notas P4**: `buildPrevReal` ganha janela [emissão→ETA] (realizado null no futuro, previsto projeta até a ETA) + KPI "X de Y dias decorridos". **Provisionamento** (rodar `GET /api/admin/provision-fleet` + cadastrar os 3 modais da VMoto): novas `fleet_supplier_modal`, `fleet_filter_preset`; colunas `sku_supplier.supplier_part_number`, `purchase_order.part_number`. **Ainda em aberto**: cabeçalho enriquecido do heatmap (KPI stockouts/VOs na janela); compra nacional emergencial automática; consolidar σ/ABC no cadastro. |
| 23 | Motor do heatmap (Projeção Global) generalizado para N modais — reverte a limitação binária do #22 | Cenários dinâmicos: baseline + 1 por modal (Courier/Aéreo/Marítimo…) + combinado; célula com arrivals por modal; simulação N-modal; snapshot regenerado | Manter binário (o que o #22 tinha decidido — reprovado: o usuário pediu explicitamente "courier + aéreo + marítimo distintos na Projeção Global"); relabelar fastest/slowest sem 3ª chegada (insuficiente) | O usuário perguntou "como coloco o courier na simulação da Projeção Global?" — o painel só tinha marítimo/aéreo porque o motor do weekgrid era binário (#22). Via AskUserQuestion ele escolheu **"motor N-modal de verdade"**. Refatorado `weekgrid.ts`: `WeekGridScenario` vira string; cenários **dinâmicos** = `baseline` + um por modal distinto (ordenado por lead) + `combined` (`scenariosFor` sobre os modais de cada SKU, do fornecedor preferido, via `modalsBySku` threaded nos inputs). `whenNeededInjection` generalizado com `pickLane(bd)`: cenário-modal injeta sempre naquele modal (pula o SKU se o fornecedor não o oferece → fica na baseline); `combined` escolhe, por ruptura, o **mais lento que chega ≤ breach**, senão o mais rápido. A **âncora mensal do marítimo morreu** — chegadas são offsets de lead puros (mudança semântica → snapshot regenerado deliberadamente). `WeekCell` trocou `arrReg/arrSug/inboundSea/inboundAir` por **`arrivals: {modal,reg,sug}[]`** (POs 'sea'/'air' legados normalizados p/ Marítimo/Aéreo via `normalizeModal`); novos `ScenarioMeta`/`ScenarioGrids {scenarios, grids}`; `buildAllScenarioGrids` retorna a lista + grids por chave e aceita `floorByScenario` (piso por modal p/ a simulação). O motor de PROJEÇÃO segue intacto (ignora `modal`; timing = eta/lead) — por isso nada disso mexe em `projectSku`/`projectStream` nem no builder do Novo Pedido. **UI** (`WeekGridView`): chips de cenário dinâmicos, células com marcador+cor por modal (🚢 Marítimo / ✈ Aéreo / 🚚 Courier), tooltip reg×sug por modal, legenda e painel de **simulação N-modal** (lead por fornecedor×modal incl. Courier + piso por modal, efêmero via server action `simulateWeekGrids`). Verificação: snapshot de caracterização regenerado (fixture ganhou fornecedor 3-modais) + **5 testes comportamentais** (conjunto de cenários; cada modal injeta pelo seu lead; combinado pega o mais lento em tempo = Aéreo, não Courier; SKU sem o modal fica na baseline; baseline nunca sugere) — 129 testes, tsc + build limpos. Sem migração nova: cadastrar os modais do fornecedor já ativa os cenários. |
| 24 | Cascata N-modal por preferência (piso+cadência por modal) na elaboração | Cascata ordenada por preferência = lead mais longo→curto (mais barato faz o volume; rápidos só cobrem o vão); tamanho = order-up-to (piso+cadência)×taxa; piso/cadência POR MODAL só na simulação (efêmero); motor único (mesmo ModalPlan no builder e no heatmap) | Persistir piso/cadência no cadastro do modal (recusado via AskUserQuestion — fica só na simulação, mantém #22); um número só por modal (recusado — o usuário quis dois: piso + cadência separados); manter a escolha gulosa antiga sem sizing por cobertura (insuficiente pro pedido "frequency = cobertura") | Follow-up do #23: o usuário pediu aéreo/marítimo uniformes como o courier + **cobertura/DOH por modal** + a engine "elaborar pedidos com frequência = cobertura e lead editável só na simulação", com **cascata por preferência** (o que o 1º-preferido resolve, compra por ele; o resto cai pro 2º; etc.; preferência = lead mais longo→curto). Travado via AskUserQuestion: (1) cobertura por modal **só simulação** (nada persistido — #22 de pé); (2) **motor único** Projeção Global + Novo Pedido; (3) **dois números por modal**: piso (DOH mín) + cadência (dias). Implementação: `whenNeededInjection` reescrito — por ruptura escolhe o modal **mais lento que chega ≤ ruptura** (senão o mais rápido, emergência) e injeta **order-up-to (piso+cadência)×taxa** no dia da chegada; piso/cadência por modal vêm de `planByModal` (só sim) ou caem no piso global + `DEFAULT_CADENCE_DAYS=30`; guarda: modal que não chega na janela não injeta. `buildAllScenarioGrids` troca `floorByScenario` por `planByModal`; coloração da célula por piso do modal (cenário-modal) ou global (baseline/combinado). **Motor único**: builder usa `suggestQuantities` (one-shot "pedir agora"), heatmap usa `whenNeededInjection` (recorrente "comprar quando necessário") — compartilham o `ModalPlan {minDoh, cadenceDays}` + a preferência longo→curto; diferem só por one-shot vs recorrente (inerente). **Simulação**: painel por (fornecedor × modal) com lead + **piso (DOH mín) + cadência (dias)** por modal, tudo efêmero. Verificação: snapshot regenerado (tamanho/lane mudam de propósito) + testes comportamentais + teste de sizing (pedido cresce com a cadência) — 130 testes, tsc + build limpos. Spec em `docs/superpowers/specs/2026-07-17-nmodal-cascade-by-preference-design.md`. **Em aberto**: preferência fixa longo→curto (sem reordenar manual ainda); persistir piso/cadência no cadastro se um dia deixar de ser só simulação. |
| 25 | Novo Pedido: piso+cadência por modal + um pedido (VO) por modal | Painel com piso (DOH mín) + cadência (dias) por modal (troca a frequência global); "Criar pedido" gera um pedido separado por modal; só o ciclo atual (1 por modal), não cronograma recorrente | Cronograma recorrente (N marítimos gerados de uma vez, recusado via AskUserQuestion — o "N marítimos" é o estado estacionário ao re-rodar); um pedido combinado agrupado por modal (recusado — o usuário quis pedidos separados); um número só de cobertura por modal (recusado — dois: piso + cadência) | Follow-up do #24 no builder do Novo Pedido. O usuário pediu: (a) periodicidade + piso DOH **por modal** (não global), em camadas — ex. Courier 15 / Aéreo 30 / Marítimo 75 (a meta); (b) **um pedido por modal**. Travado via AskUserQuestion: **só o ciclo atual** (1 pedido por modal necessário agora; o padrão "1 courier, 1 aéreo, N marítimos" é o estacionário ao re-rodar) + **um VO por modal**. Implementação em `ProcurementView`: o painel "Modais deste pedido" troca o seletor de frequência global por, por modal, `piso (DOH)` + `cadência (dias)` (blank → piso do critério global + 30d); `plans` (ModalPlan[]) passa a usar esses valores → `suggestQuantities` holda o piso de cada lane (o mais lento faz o volume order-up-to piso+cadência, os mais rápidos cobrem o vão segurando os pisos menores — a cascata em camadas). `criarPedido` agrupa as linhas por modal e chama `createPedido` uma vez por modal (VO "<nome> · <modal>"), listando os N VOs criados; o snapshot de elaboração congela piso+cadência por modal. Motor de projeção intacto; sem migração. 130 testes, tsc + build limpos. |
| 26 | Config por modal compartilhada (cookie de sessão) + lead-sim + Projeção Global só global | Cookie efêmero `vg:modalcfg` (piso/cadência/lead por fornecedor×modal) lido pelos dois tabs; lead editável só-simulação; cadência só no mais lento ("uma vez só" nos outros); Projeção Global só estoque global; mini-heatmap com DOH nas células + DOH adicionado por seta | Persistir no cadastro do modal (recusado via AskUserQuestion — fica efêmero espelhado na sessão, mantém #22); piso na janela [hoje→próximo modal] (recusado — o usuário confirmou a janela atual [chegada→próxima]); manter ABC/centros na Projeção Global (recusado); só simulação efêmera por-tab (substituída pelo cookie compartilhado) | Rodada de ajustes finos. Travado via AskUserQuestion: (1) params por modal **compartilhados via cookie de sessão** (`modalConfig.ts`: `ModalCfg` = supplierId→modalName→{piso,cad,lead}; efêmero, não-DB — reforça #22) — Novo Pedido e Projeção Global leem/escrevem o mesmo; a page da Projeção Global lê o cookie no server e passa `planByModal` + `modalsBySku` com lead-sim ao `buildAllScenarioGrids`, então os cenários "X qdo necessário" já refletem a config sem clicar nada. (2) **Lead por modal editável só na simulação** (`simLead`): entra no `suggestQuantities`/mini-heatmap, mas `createPedido` usa o lead REAL na ETA. (3) **Cadência só no modal mais lento** (por lead-sim); os outros são "uma vez só" (a cascata só usa cadência na lane sustentadora mesmo). (4) **Piso = janela [chegada do modal → próxima]** (confirmado = comportamento atual do `suggestQuantities`; sem mudança de motor). (5) **Projeção Global só estoque Global** — removidos o seletor de centros/hubs e o filtro Classe ABC. (6) **Mini-heatmap do Novo Pedido** (#7): colunas por semana com o **DOH escrito em cada quadradinho**; **▼N** (pedido registrado) e **▲N** (pedido simulado) mostram o **DOH que cada chegada adiciona** (qty÷consumo/dia) sem hover. Removida a server action efêmera `simulateWeekGrids` (o cookie + `router.refresh()` a substituem). Sem migração; motor de projeção intacto. 130 testes, tsc + build limpos. **Nuance**: Novo Pedido é one-shot (pedir agora) e a Projeção Global projeta recorrente no horizonte — mesma config/cascata, escopo temporal inerente diferente. |
| 27 | Debug do motor de pedidos: cascata N-modal re-projetada (forma fechada exata) + fixes de UI + coluna Recuperação editável inline | Cascata seed-based que RE-projeta a cada lane (substitui a `suggestQuantities` estática); sizing por **forma fechada** (continuação não-pisada com backlog); bucketing por `round`; setas laranja(registrado)/azul(novo) via ícones lucide; coluna Pedidos; coluna Recuperação editável na lista de SKUs | Manter `suggestQuantities` estática (rejeitado — não re-projeta, quebra no piso 0); fixed-point iterativo com cap (rejeitado após review — sub-dimensiona janela larga); bucketing `ceil` (rejeitado — o usuário quer a semana real da chegada); recovery só na página do SKU (rejeitado — pedido de edição inline) | Sessão de debug do engine (caso VM-07-CARA-1101). **Bug-raiz**: `suggestQuantities` dimensionava cada modal contra UMA projeção estática cujo `projectStream` **pisa o estoque em 0** (lost-sales) — uma lane-ponte cuja janela cai após a ruptura era dimensionada por "deepest-shortfall num ponto vs base pisada em 0" e as unidades injetadas depletavam ao longo da janela em vez de segurar o piso até o fim (aéreo dava ~34 DOH onde se esperava 75). **Fix**: `suggestCascadeQuantities` (em `miniStrip.ts`) re-projeta o seed a cada lane (rápido→lento): cada lane rápida segura seu piso até a chegada da próxima; a mais lenta faz order-up-to (piso+cadência). Após o review adversarial (workflow, 3 CONFIRMED + 1 falso-positivo), o fixed-point iterativo (cap 8) foi trocado por **forma fechada exata** em 1 passo: `cont(d) = stock(d) − (backlog(d) − backlog(a−1))` (continuação não-pisada a partir da chegada, sem "pagar" ruptura pré-chegada); `Q = maior (piso·taxa − cont)` na janela — sem cap, não sub-dimensiona janela larga/piso-baixo. **Motor de projeção intacto** (ignora `modal`; timing = eta/lead) → nada disso toca `weekgrid.ts`/snapshot. **Outros fixes da rodada**: (1) mojibake das setas `▼/▲` (do round-trip `unicode_escape`) → ícones lucide `ChevronDown/Up`; (2) contraste — texto preto nas células âmbar (DOH ilegível em branco); (3) bucketing das semanas do mini-heatmap `ceil`→`round` (chegada de 45d caía na semana 8, agora ~6, ancorado em hoje); (4) coluna **Pedidos** (laranja=registrado com link+ETA+qtd; azul=novo em construção) + mesma cor nas setas do heatmap; (5) POs vencidos-mas-abertos (ETA no passado) passam a aparecer em `openPos` fixados no dia 0 (igual ao `bucketReceipts`) — antes sumiam da coluna Pedidos apesar de contarem na cobertura (finding CONFIRMED); (6) desempate de lead: `slowestModalId` alinhado à regra da cascata (último após sort por lead arredondado) para a cadência não sumir num empate (finding CONFIRMED). **Coluna Recuperação** (pedido do usuário, travado via AskUserQuestion = conjunto completo): toggle recuperável + taxa% + turnaround editáveis inline na lista de SKUs, salvando por linha via a `updateRecoveryPolicy` existente (Head, auditada); origem = `snap.policies` (isRepairable resolvido = override senão stock, o que o motor usa); commit por blur/toggle com `saved`-ref anti-no-op; +recuperação no CSV/sort. Sem migração; 132 testes, tsc + build limpos. |

---

## Product Decisions

| # | Decision | Chosen | Rationale |
|---|---|---|---|
| P1 | V1 scope | Inventory table + DOH + Consumption charts | Foundation visibility layer first; forecasting and transfer suggestions require consumption time-series data not yet available |
| P2 | Consumption data source | Stub in V1, new Metabase question in V1.1 | Metabase question #27759 returns only a current snapshot (qty + DOH); a second question querying weekly SO consumption per SKU per hub is needed |
| P3 | Transfer suggestions | V2 | Requires reliable consumption time-series; adding before that creates noise rather than signal |
| P4 | Demand forecasting | V2 | Built on top of consumption time-series; Simple Moving Average (4-week window) planned |
| P5 | DOH thresholds | Warning ≥ 14 days, Critical ≤ 7 days | Baseline from similar operations; configurable via env vars (`DOH_WARNING_THRESHOLD`, `DOH_CRITICAL_THRESHOLD`) |
| P6 | Osasco recovery inflow | V2 | No current data source defined; requires a Maestro IMS query or separate Metabase question |
| P7 | Hub set | Mooca, Osasco, SBC | The 3 active maintenance hubs as of 2026; Osasco flagged as sole recovery center |

---

## Open Questions / Future Decisions

| # | Question | Status |
|---|---|---|
| Q1 | Exact column names returned by Metabase question #27759 | Obsolete — Metabase removed entirely (decision #10); superseded by direct ClickHouse reads |
| Q2 | Metabase question ID for weekly SO consumption per SKU/hub | Obsolete — superseded by `dev.sop_predictions_daily` (the S&OP forecast) read directly from ClickHouse |
| Q3 | When to add authentication | When the app URL needs to be shared more broadly or accessed from outside Vammo's network |
| Q4 | Osasco inflow data source | Maestro IMS API or new Metabase question — to be scoped in V2 |
| Q5 | DOH minimum thresholds per SKU | Currently global (14d / 7d); per-SKU thresholds may be needed as operations mature |
