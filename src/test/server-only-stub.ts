// Vitest stub for the `server-only` package (see vitest.config.ts): the real module
// throws when imported outside a React Server environment, which would make any test
// that imports a server module fail at import time. Tests only exercise pure helpers.
export {};
